import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#635bff",
      main: "#635bff",
      dark: "#121621",
      contrastText: "#ffffff",
    },
  },
});

export default theme;
